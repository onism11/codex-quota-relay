interface:
  display_name: "Codex Quota Relay"
  short_description: "Relay long tasks safely across quota resets"
  default_prompt: "Use $quota-resume-hook to 开启自动档, 关闭自动档, show status or logs, or completely uninstall Relay."
