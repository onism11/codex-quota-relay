interface:
  display_name: "Codex Quota Relay"
  short_description: "Relay long tasks safely across quota resets"
  default_prompt: "Use $quota-resume-hook to install, verify, or remove Codex Quota Relay for this project."
